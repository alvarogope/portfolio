/**
 * The site's public origin — no trailing slash. robots.txt and sitemap.xml
 * build absolute URLs from it. PLACEHOLDER: set to the real domain at deploy.
 */
export const SITE_URL = "https://example.com";
